node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
.plan
__unconfig*
todos.json

# local dev server log
dev.log

# personal resume (contains private contact details) — never commit to the public repo
resume.md
